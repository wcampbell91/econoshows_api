# from django.db import models

# class ShowBand(models.Model):
#     band = models.ForeignKey("Band", on_delete=models.CASCADE, related_name='shows')
#     show = models.ForeignKey("Show", on_delete=models.CASCADE, related_name='bands')
